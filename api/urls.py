from django.urls import path
# from .views import PostList, PostDetail

app_name = 'api'

urlpatterns = [
    # path("<int:pk>/", Template.as_view(), name="detailcreate"),
    # path("", PostList.as_view(), name="listcreate"),
]